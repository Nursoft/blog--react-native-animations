import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container: {
    paddingTop: 50,
    backgroundColor: '#F1F1F1'
  },

  listTitle: {
    fontSize: 22,
    fontWeight: '600',
    textAlign: 'center',
    marginBottom: 30
  },

  listContainer: {
    marginBottom: 50,
  },
});
